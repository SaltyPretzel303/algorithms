from setuptools import setup

setup(name='Algorithms',
      #   version='0.1',
      description='Collection of algorithms used for comparing execution times, together with some utils',
      packages=['utils', 'sorts'],
      zip_safe=False)
